spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ai_gateway
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.gateway: DEBUG
    org.springframework.data.mongodb: DEBUG
